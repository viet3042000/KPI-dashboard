package com.b4t.app.service.dto;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;
import com.b4t.app.web.rest.TestUtil;

public class CatItemDTOTest {

    @Test
    public void dtoEqualsVerifier() throws Exception {
        /*TestUtil.equalsVerifier(CatItemDTO.class);
        CatItemDTO catItemDTO1 = new CatItemDTO();
        catItemDTO1.setItemId(1L);
        CatItemDTO catItemDTO2 = new CatItemDTO();
        assertThat(catItemDTO1).isNotEqualTo(catItemDTO2);
        catItemDTO2.setId(catItemDTO1.getId());
        assertThat(catItemDTO1).isEqualTo(catItemDTO2);
        catItemDTO2.setId(2L);
        assertThat(catItemDTO1).isNotEqualTo(catItemDTO2);
        catItemDTO1.setId(null);
        assertThat(catItemDTO1).isNotEqualTo(catItemDTO2);*/
    }
}
